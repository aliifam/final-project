\section{Rumusan Masalah}
Rumusan masalah yang dapat diambil dari latar belakang yang sudah dijabarkan
sebelumnya antara lain:
\begin{enumerate}
    \item Bagaimana mengimplementasikan passwordless authentication menggunakan OAuth Google pada aplikasi e-ticketing dengan mengimplementasikan protokol OAuth 2.0 dan JSON Web Token (JWT) sebagai mekanisme autentikasi dan otorisasi.
    \item Bagaimana mengintegrasikan webhook untuk multi-channel pembayaran pada aplikasi e-ticketing dengan 3rd party payment gateway Tripay
    \item Bagaimana aplikasi e-ticketing AtleTiket ini berbeda dengan aplikasi e-ticketing yang sudah ada di pasaran dan apa keunggulan yang dimiliki oleh AtleTiket terutama dalam konteks penyelenggaraan event lari	
\end{enumerate}

\begin{lstlisting}[language=TypeScript, caption={Fungsi sederhana TypeScript untuk salam}]
const greet = (name: string): string => {
    return `Hello, ${name}!`;
};
console.log(greet("World"));
\end{lstlisting}    

\lstinputlisting[language=TSX, caption={Komponen tombol sederhana dalam React TSX}]{codes/cd.tsx}
